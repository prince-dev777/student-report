# Git and Build Publishing Restrictions

- **NO AUTOMATIC PUSH OR PUBLISH:** Do NOT run `git push` or `electron publish` under any circumstances unless explicitly and directly instructed by the user. 
  - **CRITICAL SUB-RULE:** Even if the user requested a push or build earlier in the conversation, if you have modified ANY code since that request, you MUST STOP and ask the user "Are you ready for me to build/push?" before executing the commands.
- **NO ELECTRON BUILDS:** Do NOT run `npm run build`, `npx electron-builder`, or any script that builds the Electron app, UNLESS the user explicitly and clearly tells you to do so right at the moment of execution.
- **RELEASE RULES:** When explicitly asked to build/release, always strictly adhere to the procedures and naming conventions documented in `c:\Users\sawar\MyProjects\student-report\release_Rules.md`.
- **ELECTRON ASAR CHILD PROCESS RULE:** NEVER use `child_process.spawn()` to execute Javascript/Node files that are bundled inside the `.asar` archive. Always use `child_process.fork()` instead, as `fork` is specifically patched by Electron to seamlessly extract and execute scripts from inside ASAR archives, preventing `ENOENT` crashes in production builds.
- **OMR SCANNER ABSOLUTE PROTECTION & LOCK RULE:** NEVER modify, touch, edit, calibrate, refactor, or delete ANY files inside `server/omr_scanner/`, `C:\Users\sawar\MyProjects\omrScanner\`, or `server/omr_engine_v2.py` UNLESS the user explicitly, directly, and specifically commands you to do so. All master template coordinates (T1–T7), numerical thresholds (55.0%), offsets, and visualizer modules are fully calibrated, tested across 101 real OMR scans, and permanently locked. Protect these files at all times from any accidental or unintended edits.
- **VERSION LOCK RULE (v1.0.41):** Do NOT bump or increment the app version beyond `1.0.41`. All work, fixes, and builds must strictly stay on version `1.0.41` unless the user explicitly commands a version change.
- **PYINSTALLER OMR ENGINE SYNC RULE:** In production Electron builds, the app executes the compiled standalone binary `server/omr_engine_v2.exe`. Whenever ANY modifications are made to `server/omr_engine_v2.py` or `server/omr_scanner/`, you MUST ALWAYS recompile `omr_engine_v2.exe` via PyInstaller (`cd server && pyinstaller --noconfirm --onefile --collect-all omr_scanner omr_engine_v2.py` and copy `server/dist/omr_engine_v2.exe` -> `server/omr_engine_v2.exe`) BEFORE running `npx electron-builder`. Never allow production builds to bundle an outdated Python executable.
- **MANDATORY SELF-VERIFICATION RULE:** Whenever writing, modifying, or refactoring code, you MUST independently and thoroughly verify all changes end-to-end (from frontend UI, forms, state management down to backend API endpoints, payload handling, database queries, and edge cases) via automated checks, scripts, or execution before finishing. Zero mistakes from frontend to backend.
- **WHATSAPP BOT & MESSAGING PROTECTION RULE:** NEVER write, alter, or refactor code in `whatsappClient.js`, `whatsappBotService.js`, or WhatsApp routes in a way that risks breaking initialization, session auth, message event listeners (`message`, `message_create`), or auto-reply dispatch. Any code written for WhatsApp must strictly enhance stability, preserve the active 2026 `webVersionCache`, maintain duplicate prevention, and protect error-free message delivery with 100% verified zero bugs.
- **MANDATORY SCRIPT-FIRST BUG DIAGNOSIS & DEBUGGING RULE:** Whenever the user reports ANY bug, glitch, missing data, or unexpected behavior, you MUST FIRST write and execute a standalone Python or Node.js diagnostic test script (`server/scratch/debug_*.js` / `.py`) to programmatically identify and reproduce the root cause (inspecting database collections, auth tokens, HTTP endpoints, payloads, and syntax). NEVER guess or make blind changes. After applying the fix, you MUST re-run the verification script to confirm 100% resolution with code-proven output before presenting the result to the user.
- **PERMANENT VERIFICATION SUITE PROTECTION RULE (`scripts/verify_all_api.js`):** `scripts/verify_all_api.js` is a permanent, mission-critical test suite. NEVER delete, remove, or bypass this file. It is integrated into `npm run build` and `npm run audit` as a mandatory pre-build gate to ensure all 7 core backend endpoints (Students, Attendance, Tests, Test Results, Sessions, Inquiries, SMS Logs) and MongoDB connectivity are 100% tested and operational before any build or deploy completes.
- **MANDATORY MASTER PRODUCTION AUDIT RULE (`npm run audit:master`):** Before performing any production build, packaging (`electron-builder`), or release deployment, you MUST ALWAYS execute `npm run audit:master` (`node scripts/master-production-audit.js`). This executes the comprehensive 360° 7-Stage 50+ checks pre-flight audit across: (1) Production Binaries & Bundled Assets, (2) Mobile PWA Manifests (Parent, Teacher, Staff, Inquiry), (3) Frontend React Components & Modals, (4) MongoDB Collections & Data Integrity, (5) Isolated Live Server Boot & 16 Core REST APIs, (6) Biometric Hardware Push Listeners (Ports 5000, 8000, 71), and (7) AI Knowledge Base, WhatsApp Bot, and Voice Engines. All checks must pass 100% with zero failures before proceeding. As the codebase upgrades and expands over time, `scripts/master-production-audit.js` must be continuously updated and maintained alongside new features.
